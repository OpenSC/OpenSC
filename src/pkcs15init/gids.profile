#
# PKCS15 r/w profile for SmartCard-HSM cards
#
cardinfo {
    label               = "GIDS";
    manufacturer        = "MYSMARTLOGON";

    max-pin-length      = 16;
    min-pin-length      = 6;
    pin-encoding        = ascii-numeric;
}
