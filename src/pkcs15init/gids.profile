#
# PKCS15 r/w profile for GIDS cards
#
cardinfo {
    label               = "GIDS";
    manufacturer        = "MYSMARTLOGON";

    max-pin-length      = 16;
    min-pin-length      = 4;
    pin-encoding        = ascii-numeric;
}
