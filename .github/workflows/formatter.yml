name: Check Code Style

on:
  pull_request:
    paths:
      - '**.c'
      - '**.h'
      - .github/workflows/formatter.yml

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        fetch-depth: 100
    # Uses old alpine with old clang
    # https://github.com/yshui/git-clang-format-lint/pull/4
    # - uses: yshui/git-clang-format-lint@master
    - uses: Jakuje/git-clang-format-lint@patch-2
      #with:
      #  base: origin/master
